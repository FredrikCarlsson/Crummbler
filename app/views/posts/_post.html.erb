<p>
  <h2><%= link_to post.title, post %></h2>
  <i class="text-muted small"><%= post.created_at %></i>
</p>

<p>
	<% if post.kind == 'image' %>
		<%= image_tag post.content, style: "width: 100%"%>
	<% else %>	
	<%= simple_format post.content %>
	<% end %>
</p>